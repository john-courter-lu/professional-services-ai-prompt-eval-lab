[pytest]
# Put the repo root on sys.path so `import src.*` works, and mark the rootdir so pytest
# resolves it correctly even when invoked from a subdirectory (e.g. `cd tests && pytest`).
pythonpath = .
testpaths = tests
